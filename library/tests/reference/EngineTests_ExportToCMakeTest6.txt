cmake_minimum_required(VERSION 3.16)
project(example LANGUAGES CXX)

set(SOURCE_FILES
    src/my_library.cpp
)

add_library(example STATIC
    ${SOURCE_FILES}
)

set_target_properties(example PROPERTIES
    ARCHIVE_OUTPUT_DIRECTORY $<1:${CMAKE_CURRENT_SOURCE_DIR}/../../lib>
)
